= simple_form_for @lesson, as: :lesson, url: lessons_path do |f|
  = f.error_notification
  = base_errors @lesson

  .form-inputs
    = f.input :title, label: 'Название'
    = f.simple_fields_for :pictures do |pf|
      div
        = image_tag(pf.object, height: 400) if pf.object.file?
        = pf.hidden_field :file_cache
    = f.input :files, as: :file, label: false, input_html: { multiple: true }

  .form-actions
    = f.submit 'Создать урок'
