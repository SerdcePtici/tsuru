- @title =  @story.title

= @story.title
br
span.speaker> Рассказчик:
= @story.author
br
br
#long_story_text
  = simple_format @story.text
- if can? :destroy, @story
  = button_to 'Удалить', @story, method: :delete, data: { confirm: 'Удалить историю?' }
br
- @story.pictures.each do |picture|
  div.picture = image_tag picture
  br

= render partial: 'comments/list'